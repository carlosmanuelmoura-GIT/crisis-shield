Change the title of the BIA import/export card in `src/components/sections/ImportExportSection.tsx` from:
- PT: "BIA + Plataformas" → "BIAS"
- EN: "BIA + Platforms" → "BIAs"

Only the visible card label is updated; underlying logic and column names remain unchanged.